# Common secrets
JWT_SECRET=replace-with-strong-secret

# Node gateway settings
PORT=3002
GRPC_ADDRESS=localhost:50051
WIKI_GRPC_ADDRESS=localhost:50052

# Optional proxy overrides (leave empty to disable)
# http_proxy=
# https_proxy=
# all_proxy=
# no_proxy=localhost,127.0.0.1
